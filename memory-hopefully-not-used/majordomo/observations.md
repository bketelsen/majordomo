<!-- L0: majordomo observations — append-only timestamped events -->
# Observations
- 2026-04-14 [work, insight]: Fixed onboarding wizard firing repeatedly. Root causes: (1) threshold >8 meaningful lines too high for minimal owner profile, (2) wizard-complete sentinel never written. Fixed: lowered threshold to >3 in persona-wizard.ts, added sentinel to memory/hot-memory.md.
- 2026-04-14 [insight]: Subagent runner confirmed working end-to-end. Slowness due to pi subprocess cold-start (process.argv[1] re-initializes full service stack). thinking: high agent flag is silently ignored — never passed to pi CLI. Run tracking is in-memory only, no persistence across restarts.
- 2026-04-14 [work, insight]: Full session of Majordomo development. Confirmed and fixed: (1) getPiCommand() was spawning process.argv[1] (service.ts) instead of pi binary — caused service restart on every subagent spawn, corrupted output with startup logs. Fixed to use /home/linuxbrew/.linuxbrew/bin/pi directly with PI_BIN env override. (2) Workflow pipeline validated end-to-end: 4 steps fired and chained correctly. (3) Scheduler and subagent extensions load 3x (once per domain session) — scheduler fires jobs 3x, needs fixing. (4) Added thinking blocks and tool call indicators to web UI. (5) Added workingDir to domain schema + subagent spawn. (6) Widget collapsing + sidebar scroll fixed. (7) Aspen investigation complete — worth forking, not integrating as-is.
- 2026-04-14 [work, milestone]: Architect delivered deployment architecture plan (5 docs, ~95KB, data/scratch/DEPLOYMENT_*.md). Layout: source ~/projects/sionapi, deploy ~/.local/share/majordomo/current+previous, state ~/.majordomo/ (memory sacred, data persistent). Rollback = directory swap. CLI = bin/majordomo. ~18h to implement. Key open question: bundle TS to JS or copy .ts + node_modules (recommend copy for now).
